# Pro Flight Tracker — Python Dependencies

# Web server
flask>=3.0
flask-cors>=4.0
gunicorn>=22.0

# Airport ops: lightning detection (Blitzortung WebSocket)
websockets>=12.0

# Airport ops: RVR HTML parsing
beautifulsoup4>=4.12
lxml>=5.0

# HTTP client (used by some scripts)
requests>=2.31

# Tracking store — used when DATABASE_URL is set (Railway Postgres).
# Without it the app falls back to in-memory tracking automatically.
psycopg[binary]>=3.2

# Async support for lightning WebSocket
aiohttp>=3.9
